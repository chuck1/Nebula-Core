#ifndef NEB_FND_CORE_ACTOR_ACTOR_BASE_HPP
#define NEB_FND_CORE_ACTOR_ACTOR_BASE_HPP

//#include <gal/math/pose.hpp>

#include <neb/fnd/core/actor/Base.hpp>
//#include <neb/fnd/core/actor/util/decl.hpp>

namespace neb { namespace fnd { namespace core { namespace actor { namespace actor {
	class base:
		virtual public neb::fnd::core::actor::base
	{
/*
		public:
			base();
			//base(std::shared_ptr<>);
			virtual ~base();
			//virtual void					init(neb::phx::core::actor::util::parent * const &) = 0;
			virtual void					release();
		protected:
			//void							releaseUp();
		public:	
			virtual void					step(gal::etc::timestep const & ts) = 0;
			virtual void					create_physics() = 0;
			virtual void					init_physics() = 0;
			//virtual void					add_force(real) = 0;
			//virtual void					setPose(neb::fnd::pose const & pose) = 0;
		public:
	*/
	};
}}}}}

#endif
